---
import { getAllCards, type Card } from "../../lib/firebase";
import Layout from "../../layouts/Layout.astro";
import '../../styles/cards.css';

let cards = await getAllCards();
console.log(`Cargadas ${cards.length} cartas desde Firestore (${cards.filter((c: Card) => c.type === 'major').length} mayores, ${cards.filter((c: Card) => c.type === 'minor').length} menores)`);

const pageTitle = "Cartas Aleatorias - Tarot de Marsella";
const pageDescription = "Genera una tirada aleatoria de cartas del Tarot de Marsella";

const arcanosMayores = cards.filter(card => card.type === 'major');
const arcanosMenores = cards.filter(card => card.type === 'minor');
---

<Layout title={pageTitle} description={pageDescription}>
  <main class="container mx-auto px-4 py-8 max-w-7xl">
    <header class="text-center mb-12">
      <h1 class="text-4xl font-bold text-purple-900 mb-4">Tirada de Cartas Aleatorias</h1>
      <p class="text-xl text-purple-700">Elige tu modalidad de tirada y número de cartas</p>
    </header>

    <section class="bg-white rounded-xl shadow-md p-6 mb-12 max-w-4xl mx-auto">
      <!-- Modalidad de tirada -->
      <div class="mb-6">
        <h3 class="text-lg font-semibold text-gray-800 mb-3">Modalidad de tirada:</h3>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
          <label class="flex items-center p-3 border-2 border-purple-200 rounded-lg cursor-pointer hover:bg-purple-50 transition-colors">
            <input type="radio" name="tiradaMode" value="mayores" class="mr-3 text-purple-600">
            <div>
              <div class="font-medium text-purple-800">Solo Arcanos Mayores</div>
              <div class="text-sm text-gray-600">22 cartas principales</div>
            </div>
          </label>
          
          <label class="flex items-center p-3 border-2 border-purple-200 rounded-lg cursor-pointer hover:bg-purple-50 transition-colors">
            <input type="radio" name="tiradaMode" value="mixta" class="mr-3 text-purple-600" checked>
            <div>
              <div class="font-medium text-purple-800">Tirada Mixta</div>
              <div class="text-sm text-gray-600">Mayores y menores mezclados</div>
            </div>
          </label>
        </div>
      </div>

      <!-- Controles principales -->
      <div class="flex flex-col sm:flex-row gap-4 items-end justify-center mb-4">
        <div class="flex-1 w-full sm:w-auto">
          <label for="cardCount" class="block text-sm font-medium text-gray-700 mb-1">
            Número de cartas:
          </label>
          <input
            type="number"
            id="cardCount"
            name="cardCount"
            min="1"
            max="13"
            value="3"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-purple-500 focus:border-purple-500"
          />
        </div>
        <button
          type="button"
          id="generateBtn"
          class="px-6 py-3 bg-purple-600 hover:bg-purple-700 text-white font-medium rounded-lg transition-colors"
        >
          Barajar Cartas
        </button>
        
        <button
          type="button"
          id="resetBtn"
          class="hidden px-6 py-3 bg-red-600 hover:bg-red-700 text-white font-medium rounded-lg transition-colors"
        >
          Reiniciar
        </button>
      </div>
    </section>

    <!-- Contenedor de cartas -->
    <section id="cardsContainer" class="mb-12">
      <div id="initialMessage" class="text-center py-12">
        <div class="text-6xl mb-4">🔮</div>
        <h2 class="text-2xl font-bold text-purple-900 mb-2">¿Listo para conocer tu destino?</h2>
        <p class="text-purple-700 text-lg">Haz clic en "Barajar Cartas" para comenzar tu tirada</p>
      </div>
      
      <div id="cardsGrid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 hidden">
        <!-- Las cartas se generarán aquí dinámicamente -->
      </div>
    </section>
  </main>

    <script>
        console.log('SCRIPT CARGANDO...');
        
        document.addEventListener('DOMContentLoaded', function() {
            console.log('DOM READY!');
            
            // Test inmediato
            const generateBtn = document.getElementById('generateBtn');
            console.log('Botón encontrado:', !!generateBtn);
            
            if (generateBtn) {
                console.log('Agregando click listener...');
                generateBtn.addEventListener('click', function() {
                    console.log('CLICK DETECTADO!');
                    alert('El botón funciona!');
                });
            }
        });
    </script>

    <script type="application/json" id="cardsData" set:html={JSON.stringify(cards)} />
</Layout>
